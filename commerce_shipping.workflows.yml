shipment_default:
  id: shipment_default
  group: commerce_shipment
  label: 'Default'
  states:
    draft:
      label: 'Draft'
    ready:
      label: 'Ready (to be shipped)'
    shipped:
      label: 'Shipped'
    canceled:
      label: 'Canceled'
